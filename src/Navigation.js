import React from 'react';
import './App.css';

// Simple navbar
let Navigation = () => {
    return (
        <nav className="navbar">
            <a href="#">Home</a>
            <a href="#">Venue Info</a>
            <a href="#">Tourney Signup</a>
        </nav>
    );
};

export default Navigation;
